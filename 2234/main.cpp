#include <bits/stdc++.h>

using namespace std;

int main()
{
    int totalHotDogs, participants;

    scanf("%d%d", &totalHotDogs, &participants);

    printf("%.2lf\n", (totalHotDogs + 0.0) / participants);

    return 0;
}
